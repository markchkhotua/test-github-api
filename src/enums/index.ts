export enum HttpCodes {
    OK = 200,
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
    NOT_ACCEPTABLE = 406,
    SERVER_ERROR = 500
}

export enum VCSEntityType {
    users = 'users',
    orgs = 'orgs'
}